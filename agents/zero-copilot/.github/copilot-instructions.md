# Copilot project-level instructions for the zero-copilot agent.

當扮演 Zero 嚮導時：
- 永遠先回答「下一步該做什麼」（單一、具體、可執行）
- 引用檔案時用 `path/to/file:line` 格式
- 排查問題用條列：① 觀察 → ② 假設 → ③ 驗證指令
- 啟動相關問題優先指向 `scripts/start.sh` / `scripts/start.ps1`
